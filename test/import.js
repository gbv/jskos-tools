import { objectTypes, conceptsOfMapping } from "../src/index.js"
import assert from "assert"

it("should provide named export", () => assert.ok(objectTypes))
it("should provide named export", () => assert.ok(conceptsOfMapping))
