{
  "$id": "https://gbv.github.io/jskos-json-schemas/concept.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JSKOS Concept",
  "description": "entities from a knowledge organization system",
  "type": "object",
  "allOf": [
    {
      "$ref": "item.schema.json"
    }
  ],
  "$comment": "TODO",
  "properties": {
    "narrower": {
      "description": "narrower concepts",
      "$ref": "resource.schema.json#/definitions/set",
      "$comment": "Currently allows non-concepts to be part of the set."
    },
    "broader": {
      "description": "broader concepts",
      "$ref": "resource.schema.json#/definitions/set",
      "$comment": "Currently allows non-concepts to be part of the set."
    },
    "related": {
      "description": "generally related concepts",
      "$ref": "resource.schema.json#/definitions/set",
      "$comment": "Currently allows non-concepts to be part of the set."
    },
    "ancestors": {
      "description": "list of ancestors, possibly up to a top concept",
      "$ref": "resource.schema.json#/definitions/set",
      "$comment": "Currently allows non-concepts to be part of the set."
    },
    "inScheme": {
      "description": "concept schemes or URI of the concept schemes",
      "$ref": "resource.schema.json#/definitions/set",
      "$comment": "Currently allows non-schemes to be part of the set. Question: Should a list of strings be allowed as well?"
    },
    "topConceptOf": {
      "description": "concept schemes or URI of the concept schemes",
      "$ref": "resource.schema.json#/definitions/set",
      "$comment": "Currently allows non-schemes to be part of the set. Question: Should a list of strings be allowed as well?"
    },
    "mappings": {
      "description": "mappings from and/or to this concept",
      "$ref": "resource.schema.json#/definitions/set",
      "$comment": "Currently allows non-mappings to be part of the set."
    },
    "occurrences": {
      "description": "occurrences with this concept",
      "$ref": "resource.schema.json#/definitions/set",
      "$comment": "Currently allows non-occurrences to be part of the set."
    }
  }
}